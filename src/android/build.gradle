// Geomatys fork: Crashlytics Gradle plugin disabled for AGP 8.10+ compatibility.
// The Crashlytics SDK is still included via plugin.xml frameworks, but the Gradle
// plugin (which uploads mapping files and NDK symbols at build time) is not applied.
// This is safe as long as the host app does not rely on automatic mapping/NDK upload.
